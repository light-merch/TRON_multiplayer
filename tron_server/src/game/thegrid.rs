use crate::game::arena::*;
use crate::game::*;

#[derive(Default, Debug)]
pub struct TheGrid {
    arenas: Vec<Arena>,
    accounts: AccountManager,
    inventories: InventoryManager,
}

impl TheGrid {
    fn start_arena(name: String) {
        
    }
}
